# Example environment variables for SiteAble accessibility tool
# Copy this to `.env` and fill values as needed. Do NOT commit your real keys.

# OpenAI API key (optional) — required if you use `--ai` suggestions
OPENAI_API_KEY=your-openai-api-key-here
# Preferred AI model to request (optional)
AI_MODEL=gpt-4

# Request settings
REQUESTS_TIMEOUT=10
USER_AGENT=SiteAble-Accessibility-Checker/1.0

# Logging
LOG_LEVEL=info
